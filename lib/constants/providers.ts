/**
 * Provider constants.
 */

export const PROVIDER_CATEGORIES = [
  "hosting",
  "email",
  "dns",
  "ca",
  "registrar",
] as const;

export const PROVIDER_SOURCES = ["catalog", "discovered"] as const;
