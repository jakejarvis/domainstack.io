import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Subscription",
  description: "Manage your Domainstack subscription, plan, and billing.",
};

export default function InterceptedSettingsSubscriptionPage() {
  return null;
}
